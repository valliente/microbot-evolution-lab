{
  "name": "microbot-evolution-lab",
  "private": true,
  "version": "0.1.207",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:single": "tsc && vite build --config vite.singlefile.config.ts",
    "build:package": "npm run build:single && node build-hta.js",
    "build:exe": "npm run build:single && node build-exe.js",
    "build:electron": "tsc && vite build && electron-builder --win",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide-react": "^0.344.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.2.2",
    "vite": "^5.1.6",
    "vite-plugin-singlefile": "^2.0.1"
  }
}
